Options -MultiViews
    RewriteEngine On
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.html [QSA,L]
    RewriteCond %{SERVER_PORT} 80
    RewriteRule ^(.*)$ https://d2d.pierre-monier.com/$1 [R,L]